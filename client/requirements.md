## Packages
framer-motion | Complex animations for the pitch dial and page transitions
recharts | Visualizing vocal progress and session history
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- Audio analysis uses native Web Audio API (no external lib required for MVP)
- Authentication flows redirect to /api/login and /api/logout
- "Live Coach" session mode simulates pitch detection for UI demonstration if microphone access fails
